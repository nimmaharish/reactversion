/// <reference types="react" />
interface Item {
    value: any;
    label: string;
}
export interface TabsProps {
    items: Item[];
    className?: string;
    tabClass?: string;
    activeTab?: string;
    value: any;
    onChange: (value: any) => void;
    onBack?: false | (() => void);
    size?: 'large' | 'medium';
    separator?: boolean;
}
declare function Tabs({ items, className, tabClass, activeTab, value, onChange, onBack, size, separator, }: TabsProps): JSX.Element;
export default Tabs;
