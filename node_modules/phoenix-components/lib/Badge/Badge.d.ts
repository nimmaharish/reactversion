import React from 'react';
export interface BadgeProps {
    variant?: 'primary' | 'secondary' | 'error';
    size?: 'small' | 'medium' | 'large';
    rounded?: boolean;
    children: React.ReactNode;
    className?: string;
    dot?: boolean;
    color?: 'default' | 'white';
}
declare function Badge({ variant, size, rounded, children, className, dot, color }: BadgeProps): JSX.Element;
export default Badge;
