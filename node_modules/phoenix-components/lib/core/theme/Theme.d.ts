/// <reference types="react" />
import PropTypes from 'prop-types';
import './Theme.css';
export interface ThemeProps {
    children: PropTypes.ReactNodeLike;
}
declare function Theme({ children }: ThemeProps): JSX.Element;
export default Theme;
