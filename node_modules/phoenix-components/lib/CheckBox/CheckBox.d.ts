/// <reference types="react" />
export interface CheckBoxProps {
    label: string;
    selected?: boolean;
    onChange?: (value: boolean) => void;
}
declare function CheckBox({ label, onChange, selected, }: CheckBoxProps): JSX.Element;
export default CheckBox;
