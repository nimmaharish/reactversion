/// <reference types="react" />
import { Props } from 'react-select';
export declare type SelectProps = {
    label?: string;
    error?: boolean;
    chip?: boolean;
} & Props;
declare function Select({ label, error, chip, ...props }: SelectProps): JSX.Element;
export default Select;
