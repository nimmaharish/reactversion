import React from 'react';
export interface CardProps {
    children: React.ReactNode | React.ReactNodeArray;
    outlined?: boolean;
    className?: string;
}
declare function Card({ children, outlined, className }: CardProps): JSX.Element;
export default Card;
